A simple script to invalidate (refresh the cache) AWS CloudFront objects without having to wait for the TTL to expire.

Usage: 
   1) Set your AWS credentials in config.inc. 
   2) php invalidate.php <cloundfront_distribution_id> <object_path_1,object_path_2,...>
   
Example:
   php invalidate.php XYZXYZXYZXYZXYZ /images/image1.png,/images/image2.png

   What is a path? If your object URL is http://d3hn2y9rh4o9ep.cloudfront.net/images/someimage.png, then /images/someimage.png is the path.
